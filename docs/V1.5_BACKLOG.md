# V1.5 Backlog — Etalo

Items deferred from the V1 audit perimeter for V1.5+ release.
Organised by priority.

## P1 — Must-have post-audit V1.5

### Multisig real treasuries (ADR-038)

- 2-of-3 Safe Sepolia rehearsal pre-mainnet (J11 or J12)
- 2-of-3 Safe mainnet pre-1st-real-USDT-tx (Mike hot + Mike
  hardware + 3rd-party signer TBD)
- Ownership transfer for all 6 contracts + 3 treasuries
- `docs/MULTISIG_OPS.md` operational runbook

### Indexer remaining 25 handlers (J5 Block 5b backlog)

- Voting events (`createVote`, `submitVote`, `finalizeVote`)
- Admin actions (`setEscrow`, `setStake`, etc.)
- Withdrawal lifecycle (`initiateWithdrawal`,
  `executeWithdrawal`, `cancelWithdrawal`)
- Reputation deltas (`recordCompletedOrder`, `recordDispute`,
  `checkAndUpdateTopSeller`)
- ~25 handlers to wire for full event coverage

### EIP-191 migration to on-chain events (ADR-034)

- Deprecate `lib/eip191.ts` + `app/auth.py` EIP-191 paths
- Migrate to on-chain events for authentication (extend the
  ADR-030 sole-authority pattern)
- Required before Proof of Ship submission

### Reorg-erase detection (J5 Block 5b backlog)

- Block reorg detection in indexer
- Cleanup of erased events from the mirror tables

## P2 — Nice-to-have V1.5

### Asset generator extensions (J7 backlog)

- French + Pidgin caption languages (extension of J7 Block 4)
- LinkedIn / Twitter / Pinterest templates (extension of J7 Block 2)
- Order receipt PDF / invoice for credits purchases

### Frontend polish

- Tailwind v4 upgrade (lesson J6 Block 4 — blocked while on 3.4)
- Function-component ref warning on `Button` / `DialogClose` —
  investigation
- MetaMask SDK + WalletConnect transitive warnings — webpack alias
- Cleanup test-data seed residue in dev DB

### WebSocket subscriptions (J5 backlog)

- Replace 30s polling with WebSocket subs to reduce latency

### SIWE EIP-4361 sessions (J5 backlog)

- Replace `X-Wallet-Address` auth (ADR-036) with SIWE EIP-4361
  sessions

### Top Seller / mediator rotation (J4 backlog)

- Decay scoring formula
- Periodic rotation incentive

## P3 — V2+ deferred

- KYC integration (compliance demand)
- Multi-currency (cKES, cREAL, cEUR beyond USDT)
- Buyer reputation system
- Seller scheduled posting (auto-post Instagram via API)
- Mobile native apps (React Native)
- Disputes mediator marketplace (mediator competition)
- Subgraph / indexer optimisation
- Multi-chain expansion

## Status tracking

Items marked V1.5+ shipped should be removed from this backlog.
Items requiring contract change (P1 multisig, P1 indexer,
P1 EIP-191) require an ADR before implementation. P2 and P3 items
can be implemented without ADR if they don't change architectural
invariants.
